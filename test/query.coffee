
it 'returns documents that exist when you subscribe'

it 'fetches documents in the result set'

it 'fills the documents with data when you set autoFetch = true'

it 'adds documents when its subscribed'

it 'removes documents when its subscribed'


it 'calls subscribe callback'

it 'subscribes once if you call subscribe multiple times'
