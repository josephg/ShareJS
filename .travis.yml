language: node_js
node_js:
  - 0.10

services:
  - redis-server

install:
  - npm install -g grunt-cli
  - npm install

before_script:
  - export DISPLAY=:99.0
  - sh -e /etc/init.d/xvfb start

script:
  - grunt test:server
